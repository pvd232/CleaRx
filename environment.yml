name: clearx

channels:
  - conda-forge

dependencies:
  - python=3.12
  - pip>=24
  - cmake>=3.30
  - ninja>=1.12
  - numpy>=2,<3
  - pyyaml>=6,<7
  - jsonschema>=4,<5
  - pytest>=8,<10
  - pip:
      - google-colab-cli==0.6.0
      - huggingface-hub>=0.34,<2
      - safetensors>=0.6,<1
