# AMF-PB-01

This plan adds the local contract functions, rejection tests, A100 bootstrap,
worker, and result validator for the one-case audio-memory fusion smoke. The
gate runs entirely on the local control plane; the passing review commit is the
only source accepted by the remote diagnostic.
